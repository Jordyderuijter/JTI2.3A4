#include "display.h"
#include "keyboard.h"
#include "menu.h"

// lcd_display_information(char[] *);
void menu_open_settings()
{
    lcd_clear();
    while(!kb_is_pressed(KEY_ESC))
    {
        
    }
}